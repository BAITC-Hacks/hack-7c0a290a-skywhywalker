import {patternName,levels} from '../services/labels';
import {fmt} from '../services/api';
export default function InvestigationList({ranking,selected,onSelect}) {
 return <section id="ranking" className="ranking" tabIndex={-1}><div className="section-label">ПРОВЕРИТЬ В ПЕРВУЮ ОЧЕРЕДЬ <span>{ranking.length}</span></div><p className="muted small">Чем выше балл, тем раньше стоит изучить счёт.</p><div className="ranking-scroll">{ranking.map((n,i)=><button className={`rank-row ${selected===n.node_id?'selected':''}`} key={n.node_id} onClick={()=>onSelect(n.node_id)} aria-label={`Счёт ${n.node_id}, приоритет ${fmt(n.priority_score)}`}><span className="rank-number">{String(i+1).padStart(2,'0')}</span><span className="rank-account"><b>Счёт {n.node_id}</b><small>{n.patterns[0]?patternName(n.patterns[0]):'Без заданных признаков'}</small></span><span title={`${levels[n.priority_level]} приоритет`} className={`score ${n.priority_level.toLowerCase()}`}>{fmt(n.priority_score)}</span></button>)}</div></section>
}
